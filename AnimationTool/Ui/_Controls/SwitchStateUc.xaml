<UserControl x:Name="Root"
             x:Class="AnimationTool.Ui._Controls.SwitchStateUc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AnimationTool.Ui._Controls"
             Background="{StaticResource BrushBorder3}"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="300">

    <Border Style="{StaticResource BorderArea3}"
            Background="Transparent" Height="40">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />      <!-- Text -->
                <ColumnDefinition Width="Auto" />   <!-- RadioButtons -->
            </Grid.ColumnDefinitions>

            <!-- Text -->
            <TextBlock Grid.Column="0" Margin="20 0 0 0"
                       VerticalAlignment="Center" HorizontalAlignment="Left"
                       Foreground="{StaticResource BrushBtnWhite}"
                       FontFamily="Biome"
                       FontWeight="SemiBold"
                       FontSize="20"
                       Text="{Binding Text, ElementName=Root, FallbackValue=Zoom:}" />

            <!-- RadioButtons -->
            <ListBox Grid.Column="1"
                     ItemsSource="{x:Static local:SwitchStateUc.TestItems}"
                     SelectedItem="{Binding SelectedItem, ElementName=Root}">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"></StackPanel>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <TextBlock Text="{Binding Key}"
                                               VerticalAlignment="Center" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <DataTrigger
                                Binding="{Binding IsLast}"
                                Value="{x:Null}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <TextBlock Text="{Binding Key}"
                                                       FontSize="20"
                                                       VerticalAlignment="Center" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>

            </ListBox>

        </Grid>
    </Border>
</UserControl>