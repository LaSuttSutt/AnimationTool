<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Navigation Button -->
    <Style TargetType="RadioButton" x:Key="NavigationBtn">
        <Setter Property="Height" Value="50" />
        <Setter Property="FontSize" Value="{StaticResource MediumFontSize}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="NavBorder"
                            CornerRadius="25"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BrushBtnWhite}"
                            Background="Transparent">

                        <Border x:Name="SelectionBorder" Background="Transparent" CornerRadius="25">
                            <TextBlock x:Name="NavBtnFont"
                                       Text="{TemplateBinding Content}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource BrushBtnWhite}"
                                       FontFamily="Biome"
                                       FontWeight="SemiBold"
                                       FontSize="20" />
                        </Border>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="NavBorder"
                                    Property="Background"
                                    Value="{StaticResource BrushBtnWhite}" />
                            <Setter TargetName="NavBorder"
                                    Property="BorderThickness"
                                    Value="2" />
                            <Setter TargetName="NavBorder"
                                    Property="BorderBrush"
                                    Value="{StaticResource BrushBackBlue1}" />
                            <Setter TargetName="NavBtnFont"
                                    Property="Foreground"
                                    Value="{StaticResource BrushBtnText}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SelectionBorder"
                                    Property="Background"
                                    Value="{StaticResource BrushHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Standard Button -->
    <Style TargetType="Button" x:Key="StandardBtn">
        <Setter Property="Height" Value="50" />
        <Setter Property="FontSize" Value="{StaticResource MediumFontSize}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="NavBorder"
                            CornerRadius="25"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BrushBorder2}"
                            Background="{StaticResource BrushHover}">

                        <Border x:Name="SelectionBorder" Background="Transparent" CornerRadius="25">
                            <TextBlock x:Name="NavBtnFont"
                                       Text="{TemplateBinding Content}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource BrushBtnWhite}"
                                       FontFamily="Biome"
                                       FontWeight="SemiBold"
                                       FontSize="20" />
                        </Border>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="NavBorder"
                                    Property="Background"
                                    Value="{StaticResource BrushPress}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SelectionBorder"
                                    Property="Background"
                                    Value="{StaticResource BrushHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>